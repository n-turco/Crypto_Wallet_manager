A basic C program that manages a virtual crypto wallet, it uses a menu system to perform a number of operations such as adding a transaction, printing the transactions, applying a transaction fee, finding the highest value, swapping transaction places, changes statuses on transactions, and calculating total and average dollar amounts. The program implements linked lists, memory allocation/reallocation, function pointers, bitwise operations, and basic input validation.
